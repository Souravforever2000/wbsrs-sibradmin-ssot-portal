# WBSSOT font assets

The portal uses the system-safe `Inter, Segoe UI, Arial` stack in development. Place the approved government font files in this folder and add matching `@font-face` declarations to `src/styles.css` when the distribution package is provided. No remote font dependency is required for production.
